# 示例文件

此目录用于存放测试用的 PSD/PSB 文件。

建议准备以下类型的测试文件（可用 Photopea 在线创建）：

1. `simple_banner.psd` — 简单横幅，包含：背景图层、文字图层、像素图层
2. `multi_group.psd` — 多图层组结构，包含：Header/Body/Footer 三个组，每组含多个子图层
3. `with_smartobj.psd` — 含文字图层和智能对象的模板

这些文件不应提交到 git（在 .gitignore 中排除），请本地准备。
